{
  "name": "dy-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Always-on, two-way video portal connecting two offices",
  "license": "MIT",
  "scripts": {
    "build": "npm install --prefix server",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
